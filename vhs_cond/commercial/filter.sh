# Commercial condition: brand-new sealed cassette, the way you buy it in the
# store - factory-fresh tape with a clean published recording on it. The model
# fragment already describes the factory baseline, so there is nothing to
# override - playback is the cleanest the format offers.

true